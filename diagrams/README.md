# Diagrams Notes

- Some `.mmd` files in this directory are archived renderings from earlier chain revisions.
- Diagrams marked with `HISTORICAL` include legacy chain-level `prepare_next_run` flow and are **not** current runtime policy.
- Current behavior SSOT is:
  - `chains/*.json`
  - `chain_runtime.py`
  - `docs/runbook.md`
  - `docs/chain-spec.md`
